30.Service Worker

Service Worker是一个运行在浏览器后台的可编程的网络代理，拦截并缓存请求，让页面离线可用；
它拥有独立的线程，不与页面共享生命周期，就算页面关闭也不影响线程运行；
它可以拦截所有请求，页面的每个请求都会经过它，由你编程决定从缓存拿还是走网络请求；
它具有三个生命周期钩子，install/activate和fetch，它在install里预缓存，activate里清理旧缓存，fetch里拦截每次网络请求并执行相关操作；
它能配合manifest让网页离线可用，像APP一样；

